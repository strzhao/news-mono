{
  "name": "news-mono",
  "version": "0.1.0",
  "private": true,
  "description": "AI 新闻系统 monorepo：article-db（数据上游）+ ai-news（消费层）+ cli（agent 入口）",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
